---
phase: 16-schema-hardening-critical-bug-fix
plan: 01
subsystem: database
tags: [postgres, alembic, sqlalchemy, check-constraints, index, enum]

requires:
  - phase: none
    provides: existing schema with customers, expenses, products tables
provides:
  - "Database index on customers.assigned_to for rep-based queries"
  - "Unified ExpenseCategory enum (15 values) synced between model and schema"
  - "CHECK constraints on expenses.amount, products.stock_qty, products.discount_type"
  - "Fixed return_check bug — original check status now persisted to DB"
affects: [17-backend-code-consolidation]

tech-stack:
  added: []
  patterns: ["Derive schema validation sets from model enums to prevent drift"]

key-files:
  created:
    - "backend/alembic/versions/e0b3fee5ea9f_add_assigned_to_index_check_constraints_.py"
  modified:
    - "backend/app/models/customer.py"
    - "backend/app/models/expense.py"
    - "backend/app/models/product.py"
    - "backend/app/schemas/ledger.py"
    - "backend/app/services/payment_service.py"
    - "backend/alembic/env.py"

key-decisions:
  - "Union of both enum sets for ExpenseCategory — keeps all existing DB values valid while adding new ones"
  - "Derive ALLOWED_EXPENSE_CATEGORIES from model enum import to prevent future drift"
  - "Placed update(check_txn) before create_many, consistent with deposit/undeposit patterns"

patterns-established:
  - "Schema validation sets derived from model enums: import and generate set dynamically"

requirements-completed: [SCHEMA-01, SCHEMA-02, SCHEMA-03, BACK-01]

duration: 4min
completed: 2026-03-09
---

# Phase 16 Plan 01: Schema Hardening & Critical Bug Fix Summary

**Assigned_to index, 15-value ExpenseCategory enum sync, 3 CHECK constraints, and return_check status persistence fix**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-09T11:31:32Z
- **Completed:** 2026-03-09T11:35:49Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Added database index on customers.assigned_to for rep-based query performance
- Unified ExpenseCategory enum to 15 values (union of model + schema sets) and derived schema set from model import
- Added CHECK constraints: expenses.amount > 0, products.stock_qty >= 0, products.discount_type IN ('percent','fixed')
- Fixed return_check() bug where original check's Returned status was never persisted to the database

## Task Commits

Each task was committed atomically:

1. **Task 1: Schema hardening -- index, enum sync, CHECK constraints** - `aaff31f` (feat)
2. **Task 2: Fix return_check to persist original check's Returned status** - `7c8d3be` (fix)

## Files Created/Modified
- `backend/app/models/customer.py` - Added index=True on assigned_to column
- `backend/app/models/expense.py` - Extended ExpenseCategory enum with 6 new values, added CHECK constraint on amount
- `backend/app/models/product.py` - Added CHECK constraints on stock_qty and discount_type
- `backend/app/schemas/ledger.py` - Replaced hardcoded ALLOWED_EXPENSE_CATEGORIES with model-derived set
- `backend/app/services/payment_service.py` - Added update(check_txn) call in return_check method
- `backend/alembic/env.py` - Added expense model import for autogenerate detection
- `backend/alembic/versions/e0b3fee5ea9f_...py` - Migration: index, enum values, CHECK constraints

## Decisions Made
- Used union of both enum sets for ExpenseCategory to preserve any existing DB rows with original model values
- Derived ALLOWED_EXPENSE_CATEGORIES from model enum import to prevent future drift between model and schema
- Placed update(check_txn) before create_many in return_check, consistent with deposit_check/undeposit_check patterns

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added expense model import to alembic env.py**
- **Found during:** Task 1 (migration generation)
- **Issue:** alembic/env.py was missing `import app.models.expense`, preventing autogenerate from detecting expense model changes
- **Fix:** Added the import alongside existing model imports
- **Files modified:** backend/alembic/env.py
- **Verification:** alembic autogenerate successfully detected changes
- **Committed in:** aaff31f (Task 1 commit)

**2. [Rule 3 - Blocking] Cleaned autogenerated migration to remove spurious changes**
- **Found during:** Task 1 (migration generation)
- **Issue:** Autogenerate detected unrelated changes (dropping transaction indexes, product_options nullable change) that were not part of this plan
- **Fix:** Manually wrote migration to include only index creation, enum additions, and CHECK constraints
- **Files modified:** backend/alembic/versions/e0b3fee5ea9f_...py
- **Verification:** Migration applied cleanly, all constraints verified in DB
- **Committed in:** aaff31f (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes necessary for correct migration generation. No scope creep.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Schema hardening complete with index, constraints, and enum sync
- return_check bug fixed, ready for backend code consolidation (Phase 17)
- No blockers or concerns

---
*Phase: 16-schema-hardening-critical-bug-fix*
*Completed: 2026-03-09*
