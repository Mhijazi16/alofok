---
phase: 13-offline-caching
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - frontend/src/components/shared/SyncStatusCard.tsx
  - frontend/src/components/Sales/index.tsx
  - frontend/src/locales/ar.json
  - frontend/src/locales/en.json
autonomous: true
requirements: [OFFL-03]

must_haves:
  truths:
    - "Sales rep can see last-synced time per data type (Catalog, Customers, Orders, Statements) in the Profile tab"
    - "Each data type shows a color-coded freshness dot: green (<1h), yellow (1-6h), red (>6h)"
    - "Tapping Sync Now refreshes all cached data and shows per-item animation"
    - "Pending write queue count is visible (e.g. '3 orders, 1 payment pending')"
    - "Total cache storage size is displayed"
  artifacts:
    - path: "frontend/src/components/shared/SyncStatusCard.tsx"
      provides: "Sync status dashboard card for Sales profile"
      exports: ["SyncStatusCard"]
      min_lines: 80
    - path: "frontend/src/locales/ar.json"
      provides: "Arabic sync.* locale keys"
      contains: "sync.title"
    - path: "frontend/src/locales/en.json"
      provides: "English sync.* locale keys"
      contains: "sync.title"
  key_links:
    - from: "frontend/src/components/shared/SyncStatusCard.tsx"
      to: "frontend/src/hooks/useCacheSync.ts"
      via: "useCacheSync() hook for state and syncAll action"
      pattern: "useCacheSync"
    - from: "frontend/src/components/Sales/index.tsx"
      to: "frontend/src/components/shared/SyncStatusCard.tsx"
      via: "import and render in profile view"
      pattern: "SyncStatusCard"
---

<objective>
Build the sync status card UI in the Sales profile tab showing per-data-type freshness, pending queue, cache size, and a manual Sync Now button.

Purpose: OFFL-03 requires a freshness indicator so reps know how stale their cached data is. The sync card in the profile tab gives visibility into cache state and control via manual sync.
Output: SyncStatusCard component rendered in the Sales profile tab with color-coded freshness dots, sync animation, and pending queue display.
</objective>

<execution_context>
@/home/ka1ser/.claude/get-shit-done/workflows/execute-plan.md
@/home/ka1ser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-offline-caching/13-CONTEXT.md
@.planning/phases/13-offline-caching/13-01-SUMMARY.md

@frontend/src/components/Sales/index.tsx
@frontend/src/locales/ar.json
@frontend/src/locales/en.json

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/hooks/useCacheSync.ts:
```typescript
export interface SyncTimestamps {
  products?: string;
  customers?: string;
  orders?: string;
  statements?: string;
}

export type SyncItemStatus = 'idle' | 'syncing' | 'done' | 'error';

export interface SyncState {
  timestamps: SyncTimestamps;
  statuses: Record<keyof SyncTimestamps, SyncItemStatus>;
  pendingCount: number;
  cacheSize: number;
  isSyncing: boolean;
}

export function useCacheSync(): {
  syncState: SyncState;
  syncAll: () => Promise<void>;
  syncTimestamps: SyncTimestamps;
}
```

From frontend/src/lib/syncQueue.ts:
```typescript
export const syncQueue = { push, all, remove, count };
```

From frontend/src/components/Sales/index.tsx profile section (lines ~737-847):
```tsx
case "profile":
  return (
    <div className="animate-fade-in">
      <TopBar title={t("profile.title")} />
      <div className="space-y-4 p-4">
        {/* User Card */}
        <Card variant="glass" ...>...</Card>
        {/* Settings */}
        <Card variant="glass" ...>...</Card>
        {/* SyncStatusCard goes HERE -- between Settings and Logout */}
        {/* Logout */}
        <Button variant="destructive" ...>...</Button>
      </div>
    </div>
  );
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SyncStatusCard component and add locale keys</name>
  <files>frontend/src/components/shared/SyncStatusCard.tsx, frontend/src/locales/ar.json, frontend/src/locales/en.json</files>
  <action>
1. Create `frontend/src/components/shared/SyncStatusCard.tsx`:
   - Import `useCacheSync` from `@/hooks/useCacheSync`
   - Import `useTranslation` from `react-i18next`
   - Import UI components: `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`, `Button` from `@/components/ui/button`, `Spinner` from `@/components/ui/spinner`
   - Import icons from lucide-react: `RefreshCw`, `Check`, `AlertCircle`, `Database`, `Clock`

   - Helper function `formatBytes(bytes: number): string` -- returns "X.X MB" / "X.X KB" / "X B"
   - Helper function `getRelativeTime(isoString: string | undefined, t: TFunction): string` -- returns relative time string like "2h ago", "5m ago", "just now", or t("sync.never") if undefined
   - Helper function `getFreshnessColor(isoString: string | undefined): string` -- returns:
     - `"bg-success"` if < 1 hour old
     - `"bg-warning"` if 1-6 hours old
     - `"bg-destructive"` if > 6 hours or undefined/never synced

   - Component `SyncStatusCard`:
     - Call `useCacheSync()` to get `syncState` and `syncAll`
     - Card with glass variant, matching the existing profile card style (animate-slide-up)
     - Header: CardTitle with sync icon + t("sync.title")
     - Body: list of 4 sync items (Catalog, Customers, Orders, Statements):
       - Each row: color dot (getFreshnessColor) + label + relative time on the right
       - When status is 'syncing': show Spinner instead of dot
       - When status is 'done': show green Check icon briefly (then revert to dot)
       - When status is 'error': show red AlertCircle icon
     - Separator
     - Info row: pending write queue count (e.g., "3 pending" from syncState.pendingCount) with Clock icon
     - Info row: cache size (e.g., "12.4 MB") with Database icon from syncState.cacheSize
     - Separator
     - "Sync Now" button:
       - Full width, variant="outline"
       - Disabled + shows RefreshCw spinning (animate-spin) when syncState.isSyncing
       - On click: call syncAll()
       - Label: t("sync.syncNow")

   - Design notes from CONTEXT.md specifics:
     - "feels like a dashboard within the profile -- clean, informational, not cluttered"
     - Each item shows spinner individually then flips to checkmark -- gives sense of progress
     - Pending queue display: "3 orders, 1 payment pending" format
     - Use existing design system patterns (glass card, muted-foreground text, body-sm sizing)

2. Add locale keys to `frontend/src/locales/en.json` under `"sync"` namespace:
   ```json
   "sync": {
     "title": "Data Sync",
     "catalog": "Catalog",
     "customers": "Customers",
     "orders": "Orders",
     "statements": "Statements",
     "syncNow": "Sync Now",
     "syncing": "Syncing...",
     "lastSynced": "Last synced",
     "never": "Never",
     "justNow": "Just now",
     "minutesAgo": "{{count}}m ago",
     "hoursAgo": "{{count}}h ago",
     "daysAgo": "{{count}}d ago",
     "pending": "{{count}} pending",
     "cacheSize": "Cache size",
     "syncComplete": "Sync complete",
     "syncFailed": "Some items failed to sync"
   }
   ```

3. Add locale keys to `frontend/src/locales/ar.json` under `"sync"` namespace:
   ```json
   "sync": {
     "title": "مزامنة البيانات",
     "catalog": "المنتجات",
     "customers": "الزبائن",
     "orders": "الطلبات",
     "statements": "كشوفات الحساب",
     "syncNow": "مزامنة الآن",
     "syncing": "جاري المزامنة...",
     "lastSynced": "آخر مزامنة",
     "never": "لم تتم",
     "justNow": "الآن",
     "minutesAgo": "قبل {{count}} دقيقة",
     "hoursAgo": "قبل {{count}} ساعة",
     "daysAgo": "قبل {{count}} يوم",
     "pending": "{{count}} قيد الانتظار",
     "cacheSize": "حجم التخزين",
     "syncComplete": "تمت المزامنة",
     "syncFailed": "فشلت مزامنة بعض العناصر"
   }
   ```
  </action>
  <verify>
    <automated>cd /home/ka1ser/projects/alofok/frontend && bun run build</automated>
  </verify>
  <done>SyncStatusCard component exists with 4 data-type rows with color-coded freshness dots, pending count, cache size, and Sync Now button. Both locale files have sync.* keys. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SyncStatusCard into Sales profile tab</name>
  <files>frontend/src/components/Sales/index.tsx</files>
  <action>
1. In `frontend/src/components/Sales/index.tsx`:
   - Add import: `import { SyncStatusCard } from "@/components/shared/SyncStatusCard";`
   - In the `case "profile":` block of `renderMainView()`, insert `<SyncStatusCard />` between the Settings card and the Logout button
   - Give it `className="animate-slide-up"` and `style={{ animationDelay: "90ms" }}` to fit between Settings (60ms) and Logout (120ms)
   - Update Logout button's animationDelay from "120ms" to "150ms" to accommodate the new card

2. The SyncStatusCard is self-contained -- it calls `useCacheSync()` internally. No props needed from the parent.

3. Do NOT modify any other part of the Sales index file. Only touch the profile view section.
  </action>
  <verify>
    <automated>cd /home/ka1ser/projects/alofok/frontend && bun run build</automated>
  </verify>
  <done>SyncStatusCard renders in the Sales profile tab between Settings and Logout. Build passes clean. Profile tab shows sync dashboard with freshness indicators.</done>
</task>

</tasks>

<verification>
1. `cd frontend && bun run build` passes with no TypeScript errors
2. `cd frontend && bun run lint` passes
3. Manual: Open Sales profile tab -- sync status card visible between Settings and Logout
4. Manual: Card shows 4 data types with freshness dots and relative times
5. Manual: Tap "Sync Now" -- each item shows spinner then checkmark animation
6. Manual: Pending queue count shows correct number of queued items
7. Manual: Cache size displays in human-readable format
</verification>

<success_criteria>
- SyncStatusCard component renders in Sales profile with 4 data-type rows
- Color-coded dots: green (<1h), yellow (1-6h), red (>6h stale)
- Sync Now button triggers full refresh with per-item animation
- Pending write queue count visible
- Cache storage size displayed
- Arabic and English locale keys for all sync UI strings
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-offline-caching/13-02-SUMMARY.md`
</output>
