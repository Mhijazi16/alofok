---
phase: 09-image-capture-and-ocr
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - frontend/src/components/ui/check-photo-thumbnail.tsx
  - frontend/src/components/Admin/AdminChecksView.tsx
  - frontend/src/components/Sales/StatementView.tsx
  - frontend/src/components/Customer/StatementView.tsx
autonomous: true
requirements: [IMG-03, IMG-04]

must_haves:
  truths:
    - "Check photos are viewable as thumbnails in Admin check list, Sales statement, and Customer statement"
    - "Tapping a thumbnail opens a fullscreen photo viewer"
    - "Checks without photos show no thumbnail (clean, no placeholder)"
  artifacts:
    - path: "frontend/src/components/ui/check-photo-thumbnail.tsx"
      provides: "Reusable thumbnail + fullscreen zoom overlay"
      exports: ["CheckPhotoThumbnail"]
      min_lines: 25
    - path: "frontend/src/components/Admin/AdminChecksView.tsx"
      provides: "Photo thumbnail on check cards"
      contains: "CheckPhotoThumbnail"
    - path: "frontend/src/components/Sales/StatementView.tsx"
      provides: "Photo thumbnail on check entries"
      contains: "CheckPhotoThumbnail"
    - path: "frontend/src/components/Customer/StatementView.tsx"
      provides: "Photo thumbnail on check entries"
      contains: "CheckPhotoThumbnail"
  key_links:
    - from: "frontend/src/components/ui/check-photo-thumbnail.tsx"
      to: "frontend/src/lib/image.ts"
      via: "getImageUrl for path resolution"
      pattern: "getImageUrl"
---

<objective>
Create a reusable CheckPhotoThumbnail component and add it to every view where checks appear: Admin checks list, Sales statement, and Customer statement.

Purpose: Check photos are visible everywhere checks are displayed, with tap-to-zoom for full-size viewing.
Output: Small thumbnails on check entries across all three role views, with fullscreen lightbox on tap.
</objective>

<execution_context>
@/home/ka1ser/.claude/get-shit-done/workflows/execute-plan.md
@/home/ka1ser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-image-capture-and-ocr/09-RESEARCH.md
@.planning/phases/09-image-capture-and-ocr/09-02-SUMMARY.md

@frontend/src/components/ui/check-detail-dialog.tsx
@frontend/src/components/Admin/AdminChecksView.tsx
@frontend/src/components/Sales/StatementView.tsx
@frontend/src/components/Customer/StatementView.tsx
@frontend/src/lib/image.ts
</context>

<interfaces>
<!-- Existing patterns to follow -->

From frontend/src/lib/image.ts:
```typescript
export function getImageUrl(path: string | null | undefined): string | null;
```

From frontend/src/services/salesApi.ts:
```typescript
export interface CheckData {
  check_number?: string;
  bank_name?: string;
  bank_number?: string;
  branch_number?: string;
  account_number?: string;
  holder_name?: string;
  image_url?: string;
}

export interface Transaction {
  // ...
  data: CheckData | null;
}
```

From frontend/src/components/ui/check-detail-dialog.tsx (zoom overlay pattern):
```tsx
// Existing zoom overlay pattern uses:
// fixed inset-0 z-[100] bg-black/80 with centered img, cursor-zoom-out on click
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CheckPhotoThumbnail component with zoom overlay</name>
  <files>frontend/src/components/ui/check-photo-thumbnail.tsx</files>
  <action>
Create `frontend/src/components/ui/check-photo-thumbnail.tsx`:

```typescript
interface CheckPhotoThumbnailProps {
  imageUrl: string | null | undefined;
}
```

1. Use `getImageUrl(imageUrl)` from `@/lib/image` to resolve the path
2. If resolved URL is null/falsy, return `null` (no indicator for missing photos — per discretion decision)
3. Render small thumbnail: `<img>` with classes `h-10 w-14 rounded object-cover border border-border cursor-zoom-in`
4. On click, toggle `zoomed` state
5. When zoomed, render fullscreen overlay (reuse pattern from check-detail-dialog.tsx):
   - `fixed inset-0 z-[100] flex items-center justify-center bg-black/90 cursor-zoom-out`
   - Click overlay to dismiss
   - Inner `<img>` with `max-w-[95vw] max-h-[90vh] object-contain rounded-lg`
6. Export as named export `CheckPhotoThumbnail`
  </action>
  <verify>
    <automated>cd /home/ka1ser/projects/alofok/frontend && bunx tsc --noEmit && grep -q "CheckPhotoThumbnail" src/components/ui/check-photo-thumbnail.tsx</automated>
  </verify>
  <done>CheckPhotoThumbnail renders a small thumbnail that opens a fullscreen lightbox on tap. Returns null when no image URL is provided.</done>
</task>

<task type="auto">
  <name>Task 2: Add photo thumbnails to Admin, Sales, and Customer check views</name>
  <files>frontend/src/components/Admin/AdminChecksView.tsx, frontend/src/components/Sales/StatementView.tsx, frontend/src/components/Customer/StatementView.tsx</files>
  <action>
For each of the three files, add the CheckPhotoThumbnail where check entries are rendered:

1. **AdminChecksView.tsx:**
   - Import `CheckPhotoThumbnail` from `@/components/ui/check-photo-thumbnail`
   - Find where each check card/row renders check details (likely a Card or table row mapping over checks)
   - Add `<CheckPhotoThumbnail imageUrl={check.data?.image_url} />` inline with the check info (e.g., next to check number or status badge)
   - Position it at the start or end of the check card content row

2. **Sales StatementView.tsx:**
   - Import `CheckPhotoThumbnail`
   - Find where check payment entries are rendered in the statement list
   - Check entries have `entry.type === "Check"` or similar — the `data` field contains CheckData
   - Add `<CheckPhotoThumbnail imageUrl={entry.data?.image_url} />` next to the check entry content
   - Only render for check-type entries (not cash payments or orders)

3. **Customer StatementView.tsx:**
   - Import `CheckPhotoThumbnail`
   - Same pattern as Sales StatementView — find check entries and add thumbnail
   - Add `<CheckPhotoThumbnail imageUrl={entry.data?.image_url} />` for check entries

In all three files:
- Position the thumbnail so it doesn't break existing layout — use `flex items-center gap-2` if needed
- The thumbnail is small (h-10 w-14) so it fits inline with existing content
- If entry.data is null or entry.data.image_url is undefined, CheckPhotoThumbnail returns null (no visual change)
  </action>
  <verify>
    <automated>cd /home/ka1ser/projects/alofok/frontend && bunx tsc --noEmit && bunx vite build && grep -q "CheckPhotoThumbnail" src/components/Admin/AdminChecksView.tsx && grep -q "CheckPhotoThumbnail" src/components/Sales/StatementView.tsx && grep -q "CheckPhotoThumbnail" src/components/Customer/StatementView.tsx</automated>
  </verify>
  <done>Photo thumbnails appear on check entries in Admin checks list, Sales statement, and Customer statement. Tapping opens fullscreen viewer. Checks without photos show no thumbnail. Build passes cleanly.</done>
</task>

</tasks>

<verification>
- `cd /home/ka1ser/projects/alofok/frontend && bunx tsc --noEmit` passes
- `cd /home/ka1ser/projects/alofok/frontend && bunx vite build` succeeds
- CheckPhotoThumbnail component exists and exports correctly
- All three views import and render CheckPhotoThumbnail for check entries
- Thumbnail click opens fullscreen overlay; click overlay dismisses
- Checks without image_url show no thumbnail (clean UI)
</verification>

<success_criteria>
Check photos are viewable as thumbnails across all role views (Admin, Sales, Customer). Tap-to-zoom works with a dark fullscreen overlay. No visual disruption for checks without photos.
</success_criteria>

<output>
After completion, create `.planning/phases/09-image-capture-and-ocr/09-04-SUMMARY.md`
</output>
