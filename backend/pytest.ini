[pytest]
# Single shared event loop for the whole session so the session-scoped engine
# and the function-scoped rollback session live on the same loop.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
pythonpath = .
